import React from "react";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import "./../main.css";

export const ZeroButtons = props => {
  return (
    <div className="zero-button">
      <h4>{props.zerobuttons}</h4>
    </div>
  );
};
